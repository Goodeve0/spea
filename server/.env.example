# 后端环境变量

# 数据库：dev/test 用 SQLite 文件；生产可换为 postgresql://user:pass@host:5432/db
DATABASE_URL="file:./dev.db"

# 鉴权密钥（生产务必替换为强随机值）
JWT_SECRET="dev-secret-change-me-in-production"

# HTTP API 端口（账号 / 数据持久化），与 WS_PORT(默认 3001) 区分
HTTP_PORT=3002
