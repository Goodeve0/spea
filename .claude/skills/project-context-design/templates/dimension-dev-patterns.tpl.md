<!-- AI-INSTRUCTIONS
⚠️ 开发模式维度（核心层）。

核心价值: 确保新增代码与项目现有风格一致，按标准步骤操作不遗漏。
使用方式:
  - 新增功能 → 查"修改模式"章节的对应操作手册
  - 对接 API → 查"API 契约规范"章节的统一格式
  - 理解现有功能 → 查"数据流追踪"章节的参考链路
  - 管理状态 → 查"状态管理地图"章节的组织规则

关键规则: 新增代码必须参照修改模式中的标准步骤，不得跳过任何环节。
-->

# 维度: 开发模式

最后更新: {ISO 8601}

---

## 1. 修改模式（操作手册）

<!-- PRIORITY: HIGH — 最高频使用的章节 -->

### 1.1 新增页面/路由

**标准步骤**:
1. {步骤 1，包含具体目录和文件名规则}
2. {步骤 2}
3. {步骤 3}
...

**参考文件**: `{已有页面路径}` （照此结构创建）
**注意事项**:
- {注意点 1}
- {注意点 2}

### 1.2 新增 API 接口对接

**标准步骤**:
1. {步骤}
...

**参考文件**: `{已有 API 文件路径}`
**注意事项**:
- {注意点}

### 1.3 新增业务组件

**标准步骤**:
1. {步骤}
...

**参考文件**: `{已有组件路径}`
**注意事项**:
- {注意点}

### 1.4 新增通用工具函数/Hook

**标准步骤**:
1. {步骤}
...

**参考文件**: `{已有 Hook 路径}`

### 1.5 新增状态模块

**标准步骤**:
1. {步骤}
...

**参考文件**: `{已有 Store 路径}`

## 2. API 契约规范

<!-- PRIORITY: HIGH — 新增 API 对接时必须遵循统一规范 -->

### 2.1 统一响应格式
```typescript
// 项目中标准 API 响应结构
{从已有代码中提取的通用响应类型定义，如 { code: number; data: T; message: string }}
```

### 2.2 分页规范
| 参数 | 请求字段名 | 响应字段名 | 说明 |
|------|-----------|-----------|------|
| 页码 | `{pageIndex/page}` | `{currentPage}` | {从 0 还是从 1 开始} |
| 每页条数 | `{pageSize/size}` | `{pageSize}` | {默认值} |
| 总数 | — | `{total/totalCount}` | {总记录数} |

### 2.3 错误码体系
| 错误码/范围 | 含义 | 前端处理方式 |
|------------|------|------------|
| `{code}` | {含义，如未登录/无权限/参数错误} | {跳转登录/Toast 提示/表单标红} |

### 2.4 API Mock 方案

<!-- 生成条件: 仅项目有 Mock 机制时生成，否则整节省略 -->

- **工具**: {MSW/Mock Service Worker/json-server/项目自研}
- **Mock 文件位置**: `{path}`
- **启用方式**: {环境变量/命令行参数/配置文件}

### 2.5 前后端类型同步
- **方式**: {Swagger/OpenAPI 自动生成 / 手动维护 / GraphQL Code Generator}
- **生成工具**: `{工具名}`
- **类型输出目录**: `{path}`
- **更新流程**: {步骤说明}

## 3. 数据流追踪

<!-- PRIORITY: HIGH — 理解项目数据流转的关键 -->

### 典型场景: {场景名，如列表页数据加载}

```
{用户操作}
  → {组件方法}  [{文件路径}]
  → {Hook 调用}  [{文件路径}]
  → {服务调用}  [{文件路径}]
  → {HTTP 请求}  {METHOD /api/path}
  → 响应处理  [{文件路径}]
  → {状态更新}  [{文件路径}]
  → {UI 刷新}
  → {副作用: 埋点/缓存/...}  [{文件路径}]
```

**链路说明**:
- {关键节点的补充说明}

（每个典型场景一个追踪图，建议 2-3 个覆盖：查询型、提交型、导航型）

## 4. 状态管理地图

### 4.1 方案概览
- **方案**: {Redux/Zustand/Pinia/Context 等}
- **Store 入口**: `{path}`
- **全局 vs 局部判断标准**: {规则}

### 4.2 Store 结构

| 模块/Slice | 路径 | 核心状态 | 职责 |
|-----------|------|---------|------|
| `{name}` | `{path}` | `{key states}` | {职责} |

### 4.3 数据缓存策略

| 数据类型 | 缓存方式 | 缓存位置 | 失效策略 |
|---------|---------|---------|---------|
| {类型} | {内存/localStorage/sessionStorage} | {位置} | {TTL/手动/切换页面} |

## 5. 代码惯例（源码索引）

<!-- 
生成说明: 不要在此写骨架代码。AI Agent 可直接读取参考文件获取真实实现。
每种惯例只需列出: 模式名、参考文件路径、关键函数/类名、要点摘要。
-->

| 惯例模式 | 参考文件 | 关键函数/类 | 要点 |
|---------|---------|-----------|------|
| {组件编写} | `{典型组件文件路径}` | `{组件名}` | {如: Props 接口 + Hook 驱动 + 条件渲染} |
| {Hook 编写} | `{典型 Hook 文件路径}` | `{Hook 名}` | {如: 状态+请求+返回值解构} |
| {服务层封装} | `{典型服务文件路径}` | `{函数名}` | {如: withProjectRoot 包裹 + log 首尾} |
| {API Route} | `{典型 Route 文件路径}` | `{handler 名}` | {如: SSE TransformStream + onEvent} |
| {测试编写} | `{典型测试文件路径}` | `{测试函数名}` | {如: CommonJS + assert + async} |

> 💡 需要了解具体实现时，直接读取"参考文件"列中的源码文件即可获得真实的、最新的代码模式。
