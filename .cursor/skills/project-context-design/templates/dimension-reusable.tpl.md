<!-- AI-INSTRUCTIONS
⚠️ 可复用能力维度（核心层）。

核心价值: 避免重复造轮子，直接复用已有的工具函数、Hook、服务、组件。
使用方式: 写代码前先搜索此文件，看是否已有满足需求的能力。
关键规则: 如果已有能力能满足 80% 需求，优先复用并扩展，而非新建。
-->

# 维度: 可复用能力

最后更新: {ISO 8601}

---

## 1. 通用工具函数

<!-- PRIORITY: HIGH — 最常被复用的能力 -->

| 函数 | 路径 | 签名 | 用途 | 复用度 |
|------|------|------|------|--------|
| `{name}` | `{path}` | `{(params) => return}` | {场景} | {高/中/低} |

## 2. 自定义 Hooks

| Hook | 路径 | 签名 | 用途 | 复用度 |
|------|------|------|------|--------|
| `{name}` | `{path}` | `{(params) => { returns }}` | {场景} | {高/中/低} |

## 3. 服务/API 封装

| 服务 | 路径 | 主要方法 | 用途 |
|------|------|---------|------|
| `{name}` | `{path}` | `{methods}` | {场景} |

### 基础请求封装
- **封装位置**: `{path}`
- **使用方式**: {如何发起请求}
- **内置能力**: {token 注入/错误拦截/重试 等}

## 4. UI 组件

### 4.1 通用组件

| 组件 | 路径 | 核心 Props | 用途 |
|------|------|-----------|------|
| `{name}` | `{path}` | `{key props}` | {场景} |

### 4.2 业务组件

| 组件 | 路径 | 核心 Props | 用途 |
|------|------|-----------|------|
| `{name}` | `{path}` | `{key props}` | {场景} |

## 5. 中间件/拦截器/装饰器

| 名称 | 路径 | 类型 | 用途 |
|------|------|------|------|
| `{name}` | `{path}` | {中间件/拦截器/装饰器/HOC} | {场景} |

## 6. 第三方库使用指南

<!-- 项目中引入的第三方库的标准用法，避免不一致的使用方式 -->

| 库 | 版本 | 项目中的使用方式 | 封装位置 | 注意事项 |
|----|------|----------------|---------|---------|
| `{name}` | `{ver}` | {如何使用} | `{封装路径}` | {限制/约定} |
