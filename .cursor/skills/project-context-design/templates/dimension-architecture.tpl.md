<!-- AI-INSTRUCTIONS
⚠️ 架构与规范维度（核心层）。

核心价值: 确保你写的代码放在正确的位置、遵循正确的规范。
使用方式: 新建文件前查目录约定，写代码前查编码规范，引入配置前查环境管理。
-->

# 维度: 架构与规范

最后更新: {ISO 8601}

---

## 1. 技术架构详解

### 1.1 架构模式
{详细的架构描述，包括分层策略、模块划分原则}

### 1.2 目录结构约定

<!-- PRIORITY: HIGH -->

| 目录 | 职责 | 命名规范 | 示例 |
|------|------|---------|------|
| `{path}` | {职责} | {命名规则} | `{示例文件}` |

### 1.3 构建与部署

| 环节 | 工具/方式 | 配置文件 | 备注 |
|------|----------|---------|------|
| 构建 | {工具} | `{配置路径}` | {备注} |
| 部署 | {方式} | `{配置路径}` | {备注} |

## 2. 编码规范

<!-- PRIORITY: HIGH -->

### 2.1 文件命名
- 组件文件: {规则，如 PascalCase.tsx}
- 工具文件: {规则，如 kebab-case.ts}
- 样式文件: {规则，如 index.module.less}
- 测试文件: {规则，如 *.test.ts / *.spec.ts}

### 2.2 代码风格
- 缩进: {tab/2空格/4空格}
- 引号: {单引号/双引号}
- 分号: {有/无}
- import 排序: {规则}
- 导出方式: {named export / default export 偏好}

### 2.3 TypeScript 规范
- strict 模式: {是/否}
- 路径别名: {@ → src/ 等}
- 类型定义位置: {集中 types/ vs 就近 *.d.ts}
- any 使用策略: {禁止/受限/自由}

## 3. 质量门禁

| 门禁 | 工具 | 配置 | 触发时机 |
|------|------|------|---------|
| lint | {工具} | `{配置路径}` | {pre-commit/CI} |
| 格式化 | {工具} | `{配置路径}` | {保存时/pre-commit} |
| 类型检查 | {tsc} | `{tsconfig}` | {CI} |
| 测试 | {框架} | `{配置路径}` | {CI} |

## 4. 环境与配置管理

<!-- PRIORITY: HIGH — 新功能常需引入新配置，必须遵循项目规范 -->

### 4.1 环境变量体系
| 环境 | 配置文件 | 用途 | 加载优先级 |
|------|---------|------|-----------|
| {dev/staging/prod} | `{.env.development}` | {本地开发} | {优先级} |

### 4.2 配置注入方式
- **构建时注入**: {前缀规则，如 `VITE_*` / `NEXT_PUBLIC_*` / `REACT_APP_*`}
- **运行时读取**: {方式，如 `window.__CONFIG__` / API 获取 / 配置中心}
- **关键文件**: `{配置读取封装位置}`

### 4.3 特性开关

<!-- 生成条件: 仅项目有 Feature Flags 机制时生成，否则整节省略 -->

- **方案**: {环境变量/配置中心/自研/LaunchDarkly}
- **使用方式**: `{调用示例}`

### 4.4 配置安全规范
| 类别 | 规范 | 示例 |
|------|------|------|
| 禁止前端包含 | {密钥/私钥/数据库连接串等} | {反例} |
| 敏感配置处理 | {通过后端 API 获取/环境变量注入/加密存储} | {正例} |

## 5. 性能与安全约束

### 性能预算
| 指标 | 限制 | 测量方式 |
|------|------|---------|
| {指标} | {限制} | {工具/方式} |

### 安全策略
| 策略 | 实现方式 | 配置位置 |
|------|---------|---------|
| {策略} | {实现} | `{路径}` |

### 兼容性要求
| 维度 | 要求 |
|------|------|
| {浏览器/Node/平台} | {具体要求} |
