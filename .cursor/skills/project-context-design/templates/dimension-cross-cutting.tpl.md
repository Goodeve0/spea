<!-- AI-INSTRUCTIONS
⚠️ 横切关注点维度（按需层）。

核心价值: 确保新功能正确接入鉴权、错误处理、埋点等系统级能力，不遗漏。
使用方式: 新增页面或功能后，逐项检查本文件的清单，确认每项是否需要接入。
加载时机: 新增页面/功能、需要接入系统级能力时加载。

接入检查清单:
- [ ] 鉴权/权限: 是否需要登录态？是否需要特定权限？
- [ ] 错误处理: 接口错误/渲染错误如何提示用户？
- [ ] 埋点监控: 页面 PV 和关键操作是否需要埋点？
- [ ] 样式方案: 是否使用正确的样式方案和主题变量？
- [ ] 国际化:   文案是否需要国际化？（如项目需要）
-->

# 维度: 横切关注点

最后更新: {ISO 8601}

---

## 1. 鉴权/权限

<!-- PRIORITY: HIGH -->

### 1.1 登录态
- **获取方式**: {Token/Cookie/Session}
- **存储位置**: {localStorage/cookie/内存}
- **注入方式**: {HTTP 拦截器自动注入 / 手动传参}
- **关键文件**: `{path}`

### 1.2 权限校验
- **校验层级**: {路由级/页面级/组件级/接口级}
- **实现方式**: {路由守卫/HOC/Hook/指令}
- **关键文件**: `{path}`

### 1.3 新功能接入步骤
1. {步骤}
...

## 2. 错误处理

### 2.1 全局错误捕获
- **渲染错误**: {ErrorBoundary 位置和行为}
- **接口错误**: {拦截器位置和统一格式}
- **未捕获异常**: {window.onerror / unhandledrejection}
- **关键文件**: `{path}`

### 2.2 统一错误格式
```typescript
{项目中的错误格式定义}
```

### 2.3 用户提示方式
| 场景 | 提示方式 | 调用方法 |
|------|---------|---------|
| 接口错误 | {Toast/Modal} | `{调用方式}` |
| 表单校验 | {inline/Toast} | `{调用方式}` |
| 页面崩溃 | {兜底页面} | `{自动触发}` |

## 3. 监控/埋点/日志

### 3.1 方案
- **SDK**: {名称和版本}
- **初始化位置**: `{path}`

### 3.2 标准埋点
| 类型 | 触发方式 | 标准参数 | 接入方式 |
|------|---------|---------|---------|
| 页面 PV | {自动/手动} | `{params}` | {HOC/Hook/装饰器} |
| 点击事件 | {手动} | `{params}` | `{调用方式}` |
| 接口监控 | {自动} | `{params}` | {拦截器自动} |

### 3.3 新功能接入步骤
1. {步骤}
...

## 4. 国际化

<!-- 生成条件: 仅项目有 i18n 需求时生成，否则整节省略 -->

- **方案**: {i18next/vue-i18n/自定义}
- **文案文件位置**: `{path}`
- **新增文案步骤**: {步骤}

## 5. 样式方案

### 5.1 技术选择
- **主方案**: {CSS Modules/Styled-Components/Tailwind/Less/Sass/行内样式/CSS-in-JS}
- **UI 组件库**: {Ant Design/MUI/Element Plus/无}（版本）
- **主题变量位置**: `{path}`
- **设计 Token**: {是否使用/位置}

### 5.2 样式编写规范

| 场景 | 推荐方式 | 示例/说明 |
|------|---------|----------|
| 布局组件 | {UI 库组件/Flex 工具类/自定义} | {如 `<Row>` `<Col>` 或 Tailwind `flex`} |
| 业务组件自定义样式 | {CSS Modules/行内 style/styled} | {如 `styles.container` 或 `style={{ padding: 16 }}`} |
| UI 库组件定制 | {主题覆盖/className 覆盖/style 属性} | {如 ConfigProvider theme 或 className 覆盖} |
| 颜色/间距等 Token | {主题变量/常量文件/硬编码} | {如 `token.colorPrimary` 或 `styles/variables.ts`} |
| 图标 | {UI 库图标/自定义 SVG/iconfont} | {如 `@ant-design/icons` 或 `assets/icons/`} |

### 5.3 样式常量与工具

<!-- 生成条件: 仅项目有样式常量文件时生成 -->

| 文件 | 内容 | 用途 |
|------|------|------|
| `{path}` | {导出的常量/对象名} | {如按钮样式、排版样式、主题 token} |

### 5.4 多端/响应式适配

<!-- 生成条件: 仅多端或响应式项目生成 -->

| 平台/断点 | 适配方式 | 关键文件 |
|-----------|---------|---------|
| {平台/断点} | {方式} | `{path}` |

## 6. 副作用地图

<!-- PRIORITY: HIGH — 新功能常因遗漏副作用接入而产生 Bug -->

| 副作用 | 位置 | 触发时机 | 做什么 | 新功能是否需要关注 |
|--------|------|---------|--------|-------------------|
| HTTP 拦截器 | `{path}` | {每次请求} | {token 注入/错误捕获} | {自动生效/需手动} |
| 路由守卫 | `{path}` | {路由切换} | {登录检查/权限校验} | {自动生效/需配置} |
| 全局事件 | `{path}` | {特定事件} | {具体行为} | {按需监听} |
| {其他} | `{path}` | {时机} | {行为} | {是否需关注} |
