# AGENTS.md · AI 编码助手规则

> 本文件会被主流 AI 编码工具自动加载（Cursor / Claude Code / Copilot 等）。
> 它是 AI 在本仓库内的**行为宪法**。任何编码动作都必须遵守。

---

## 0. 黄金法则（违反即任务失败）

1. **Spec 先行**：动手写代码前，先读 `specs/` 下相关文档（至少 `00-AI-HARNESS.md` + 对应 Feature Spec）。Spec 没定义的，不要自由发挥。
2. **测试驱动**：先写测试（Red）→ 再写实现（Green）→ 重构（Refactor）。见 `specs/03-TDD-GUIDE.md`。
3. **小步可控**：一次只做一个 `specs/04-TASKS.md` 里的任务，只改任务涉及的文件。
4. **可验证交付**：每次产出都附上"如何运行测试验证"的命令和结果。

---

## 1. 开始任何任务前的自检（AI 必须先回答）

```
□ 我读了哪个 Spec？要实现的接口契约是什么？（复述一遍）
□ 这个任务对应 TASKS 里的哪个 T-XX？
□ 验收标准 AC 有哪几条？我会用哪些测试覆盖？
□ 我会改动哪些文件？是否超出任务范围？
```
> 不能清晰回答以上问题时，**先提问，不要写代码**。

---

## 2. 必须遵守的硬约束（MUST）

- **不改 Spec 之外的范围**；不顺手"优化"无关代码。
- **不删/改测试来让测试通过**；测试是契约，改测试须先改 Spec。
- **不臆造接口**；调用任何函数/类型前，先确认其真实签名（读 `shared/contracts.ts` 或源码）。
- **不引入未在 `02-SDD.md` 登记的依赖**。
- **不写占位/TODO/空实现蒙混**；要么完成，要么明确报告无法完成及原因。
- **不硬编码密钥**；一律走环境变量（见 `.env.example`）。
- **不滥用 `any` / `@ts-ignore`** 绕过类型检查。
- **前后端共享类型只从 `shared/contracts.ts` import**，不重复定义。

---

## 3. 代码风格

| 项 | 约定 |
|----|------|
| 语言 | TypeScript（strict 模式） |
| 命名 | 文件 kebab-case；类型/类 PascalCase；变量/函数 camelCase；常量 UPPER_SNAKE |
| 函数 | 单一职责，尽量纯函数；副作用隔离到边界 |
| 依赖注入 | service 通过构造函数注入依赖（便于 mock 测试） |
| 错误处理 | 不吞错（除非 Spec 明确要求降级）；错误信息要可定位 |
| 注释 | 解释"为什么"，不解释"是什么"；公共接口写 JSDoc |
| 提交信息 | `feat(模块): 描述 (T-XX, US-XX)`，引用任务/需求编号 |

---

## 4. 标准工作循环

```
1. 读 Spec        → 复述契约，不写代码
2. 写测试 (Red)   → 覆盖正常/边界/异常，运行确认失败
3. 写实现 (Green) → 最小实现让测试通过，只改指定文件
4. 重构           → 保持测试全绿
5. 自检 DoD       → 对照 00-AI-HARNESS 第 5 节逐条核对
6. 总结提交       → 3-5 行总结改动 + 更新 TASKS 状态
```

---

## 5. 卡住时（Escalation）

- 同一测试连续 3 次没修复 → 停手，回读 Spec 检查是否理解错契约。
- Spec 有歧义 → 停手提问，更新 Spec 后再继续。
- 任务太大 → 拆更小，更新 `specs/04-TASKS.md`。
- 依赖/环境问题 → 报告具体错误，**不要 hack 绕过**。

> 原则：**宁可停下来问清楚，也不要瞎改制造技术债。**

---

## 6. 项目专属上下文

- 项目：AI 英语口语陪练（黑客松题目一），详见 `英语口语陪练-技术方案.md`。
- 架构与接口契约：`specs/02-SDD.md`（唯一事实来源）。
- 数据模型与消息格式：`shared/contracts.ts`。
- 测试框架：Vitest。运行：`npm test`。
- 外部 AI（OpenAI/Azure）调用一律可被 mock，测试不依赖真实网络。

---

## 7. 关键命令

```bash
npm install              # 安装依赖
npm test                 # 运行全部测试
npm test -- <file>       # 运行单个测试文件
npm run test:coverage    # 覆盖率
npm run lint             # 代码检查
npm run dev              # 启动开发（前/后端）
```

---

## 8. 文档索引

| 想知道 | 看 |
|--------|----|
| 整体工作流/护栏 | `specs/00-AI-HARNESS.md` |
| 做什么、为谁做 | `specs/01-PRD.md` |
| 怎么设计、接口契约 | `specs/02-SDD.md` |
| 怎么写测试 | `specs/03-TDD-GUIDE.md` |
| 现在做哪个任务 | `specs/04-TASKS.md` |
| 某个功能的精确契约 | `specs/features/F*.md` |
| 技术选型背景 | `英语口语陪练-技术方案.md` |
